name: Build Dev Containers

on:
  push:
    branches:
      - main
    tags:
      - "v*"

permissions:
  contents: read
  packages: write

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ${{ github.repository_owner }}/antwika-dev

jobs:
  build-base:
    name: Build base dev container
    runs-on: ubuntu-latest

    outputs:
      version: ${{ steps.version.outputs.version }}

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Get version
        id: version
        run: |
          if [[ "$GITHUB_REF" == refs/tags/v* ]]; then
            VERSION=${GITHUB_REF#refs/tags/v}
          else
            VERSION=${GITHUB_SHA}
          fi
          echo "version=$VERSION" >> "$GITHUB_OUTPUT"

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push base image with version tag
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/base/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-base:${{ steps.version.outputs.version }}

      - name: Build and push base image with latest tag
        if: startsWith(github.ref, 'refs/tags/v')
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/base/Dockerfile
          push: true
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-base:latest
  build-llvm:
    name: Build LLVM devcontainer
    needs: build-base
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.build-base.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push LLVM image with version tag
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/llvm/Dockerfile
          push: true
          build-args: |
            BASE_VERSION=${{ env.VERSION }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-llvm:${{ env.VERSION }}

      - name: Build and push LLVM image with latest tag
        if: startsWith(github.ref, 'refs/tags/v')
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/llvm/Dockerfile
          push: true
          build-args: |
            BASE_VERSION=${{ env.VERSION }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-llvm:latest

  build-gnu:
    name: Build GNU devcontainer
    needs: build-base
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.build-base.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push GNU image with version tag
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/gnu/Dockerfile
          push: true
          build-args: |
            BASE_VERSION=${{ env.VERSION }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-gnu:${{ env.VERSION }}

      - name: Build and push GNU image with latest tag
        if: startsWith(github.ref, 'refs/tags/v')
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/gnu/Dockerfile
          push: true
          build-args: |
            BASE_VERSION=${{ env.VERSION }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-gnu:latest

  build-mingw:
    name: Build MinGW devcontainer
    needs: build-base
    runs-on: ubuntu-latest
    env:
      VERSION: ${{ needs.build-base.outputs.version }}
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push MinGW image with version tag
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/mingw/Dockerfile
          push: true
          build-args: |
            BASE_VERSION=${{ env.VERSION }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-mingw:${{ env.VERSION }}

      - name: Build and push MinGW image with latest tag
        if: startsWith(github.ref, 'refs/tags/v')
        uses: docker/build-push-action@v6
        with:
          context: .
          file: .devcontainer/mingw/Dockerfile
          push: true
          build-args: |
            BASE_VERSION=${{ env.VERSION }}
          tags: |
            ${{ env.REGISTRY }}/${{ env.IMAGE_PREFIX }}-mingw:latest
